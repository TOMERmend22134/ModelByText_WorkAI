import { predictGamePublisher } from './functions';

// The game name you want to predict the publisher for
const gameName = 'Call of Shenkar';

// Call the function to predict the publisher and log it to the console
predictGamePublisher(gameName);